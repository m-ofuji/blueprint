import { MAIN_COLOR, SG_HOLD_COLOR, HOLD_COLOR, TARGET_COLOR } from "./Colors"
import { GRADES } from "./Grades"
import { MAX_SIDE_LENGTH } from "./MaxSideLength"

export {
  MAIN_COLOR,
  SG_HOLD_COLOR,
  HOLD_COLOR,
  TARGET_COLOR,
  GRADES,
  MAX_SIDE_LENGTH
}